# Direction

Selected: restrained physical response. Compression is fast, release settles softly, and large spatial movement carries more mass. Rejected: elastic/bouncy motion across every component, because repeated IDE interactions would become tiring.
