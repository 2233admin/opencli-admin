# Brief

Replace the single login background with three switchable, fluid/pixel themes built from registry components. Keep authentication untouched, default to fluid, preserve reduced motion, and avoid custom shader work.
