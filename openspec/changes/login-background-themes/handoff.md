# Handoff

Complete with one licensed-component fallback. Pixel Liquid is the default; Terminal and Matrix Pixel are selectable. When `UNLUMEN_LICENSE_KEY` is provided, install `@unlumen-ui/pixel` and replace only the `theme === 'pixel'` branch in `frontend/app/login/page.tsx`.
