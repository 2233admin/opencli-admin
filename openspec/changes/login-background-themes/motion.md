# Motion

Theme changes are occasional, so a 450ms zero-bounce spring is acceptable. Entry materializes from 6px blur and 1.01 scale; exit is smaller. Reduced-motion removes entry transformation and disables ambient auto-animation where supported.
