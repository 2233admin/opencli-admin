# Directions

## Selected: quiet operational chart

Use the official Tremor Raw AreaChart composition as the interaction and visual reference while retaining the repository's shadcn tokens. Keep the chart readable and controls explicit; performance instrumentation is a verification mode, not permanent dashboard chrome.

## Rejected: decorative animated dashboard

Animating all 5,000 SVG samples continuously would measure path churn rather than the product's real streaming behavior and would reduce operator readability.
