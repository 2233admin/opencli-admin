# Present installed tools as Workflow nodes

Integration Packages are the installation and update unit, Tools are their callable capabilities, and Nodes are how those capabilities participate in a Workflow. Even when a Node encapsulates multiple internal steps, the product presents it simply as a Node and does not expose implementation labels such as HDA, recipe, or composite node.
