# Keep the data plane local-first with explicit cloud processing

Collected data, processing outputs, indexes, Artifacts, and Data Feeds remain in the user's environment by default. Local and cloud providers implement the same node contracts, but any step that sends data to an external provider is explicit and visible in the Workflow and Run evidence; local-first does not mean offline-only or cloud-unsupported.
