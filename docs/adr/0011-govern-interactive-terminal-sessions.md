# Govern interactive terminal access as Control Sessions

Interactive terminal access is opened by a Control Action as a time-limited and scope-limited Control Session. The session records commands and outputs as evidence. Approval applies to the bounded session rather than every command, while Agents and humans share the same authorization path and neither receives permanent or unaudited shell access.
