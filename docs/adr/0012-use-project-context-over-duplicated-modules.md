# Use Project context instead of duplicated project modules

A Project provides a focused home for its Workflows, recent results, Data Feeds, Automations, and Triage. Shared modules such as data sources, runs, devices, workers, and integrations remain global and accept the active Project as a removable filter. The product does not maintain a second project-specific administration surface for the same resources.
