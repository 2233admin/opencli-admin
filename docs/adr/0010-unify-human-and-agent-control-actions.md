# Unify human and Agent operations as Control Actions

Human controls and Agent commands create the same structured Control Action. Every action uses the same authorization and risk policy, enters Gate and Inbox when approval is required, and reaches side effects only through the Actuator. Device-level UI and Agent conversations are initiation surfaces; the global control view is the shared history and audit surface, not a second execution system.
