# Defer Hyperspace behind an explicit Integration Package

Hyperspace is a supported future Integration Package rather than core infrastructure or a universal Node. Its distinct inference, embedding, proxy, storage, and memory capabilities may become separate Nodes when demanded. Local and P2P execution remain explicit modes, external network transmission follows Workspace data-egress policy, and the platform does not redistribute an unlicensed daemon or reimplement its unpublished peer protocol.
